# reports/ 目錄說明

此目錄用於存放塔台/工單執行過程中的研究輸出、稽核報告與暫存分析文件。

## Git 狀態
`reports/` 目錄預設被 `.gitignore` 排除，避免將高噪音研究草稿與中間產物納入版本控制。

本檔案以例外規則保留在 repo，目的是讓協作者知道此目錄用途與忽略策略。

## 建議使用
1. 可重建或一次性分析產物放 `reports/`
2. 需要長期追蹤的決策結論，請整理後寫回 `_tower-state.md` / `_learnings.md` / 工單檔案
